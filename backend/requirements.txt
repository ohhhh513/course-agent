fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
PyJWT>=2.8
python-multipart>=0.0.9   # 支持 /question/materials 文件上传
httpx>=0.27                # 测试用（TestClient 依赖）
# 数据库（真实库表）
SQLAlchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1      # 生产用 Postgres 驱动（本地 SQLite 默认不需要）
